# Local Postgres (used by docker-compose.yml)
POSTGRES_DB=alltix_oms
POSTGRES_USER=alltix_admin
POSTGRES_PASSWORD=change_me_admin

# Migration role: owns the schema, must NOT be used by application code
# (Postgres does not apply RLS policies to table owners/superusers).
DATABASE_URL=postgres://alltix_admin:change_me_admin@localhost:5432/alltix_oms

# Least-privilege role every service connects as. RLS policies are enforced
# for this role. Created by packages/db/migrations/0001_extensions_and_roles.sql.
APP_USER_PASSWORD=change_me_app
APP_DATABASE_URL=postgres://app_user:change_me_app@localhost:5432/alltix_oms

# packages/web -- Clerk auth (get real values from the Clerk dashboard). The
# publishable key below is a structurally-valid but fake placeholder (Clerk
# validates key *format* on every request, before it even checks whether a
# route is public), so leaving it as an arbitrary string like
# "pk_test_replace_me" 500s every request including the e2e test's.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZmFrZS10ZXN0LWFwcC5jbGVyay5hY2NvdW50cy5kZXYk
CLERK_SECRET_KEY=sk_test_replace_me
# Signing secret for the /api/webhooks/clerk endpoint (Clerk dashboard ->
# Webhooks -> your endpoint -> Signing Secret). Required so the webhook route
# can verify inbound payloads are actually from Clerk before provisioning a
# tenant (CLAUDE.md §6: never trust an unsigned webhook payload).
CLERK_WEBHOOK_SECRET=whsec_replace_me

# packages/web -- test-only bypass of Clerk auth, used exclusively by
# test/tenant-isolation.e2e.test.ts to simulate two different signed-in users
# via the x-test-clerk-user-id header instead of real Clerk sessions. Only
# honored when NODE_ENV !== "production" (see src/lib/auth-context.ts) --
# `next build`/`next start` force NODE_ENV to "production" regardless of this
# file, so leave this unset/false everywhere except the e2e test's own
# environment, and never set it in a deployed environment's config.
ALLTIX_TEST_AUTH_BYPASS=false

AMAZON_SANDBOX_CLIENT_ID=
AMAZON_SANDBOX_CLIENT_SECRET=
AMAZON_SANDBOX_REFRESH_TOKEN=
# Seller/Merchant ID -- required as a path segment by the Listings Items API
# (AmazonConnector.pushInventory). The SP-API sandbox doesn't validate this
# value against anything real, so any non-empty string works there; a real
# tenant's actual value is captured during OAuth onboarding and belongs in
# channel_connections.external_account_id, not this env var.
AMAZON_SANDBOX_SELLER_ID=

# Arif's own real Seller Central app credentials -- completely separate
# from AMAZON_SANDBOX_* above, never mix them. Used only by
# scripts/amazon-production-smoke-test.ts (loadAmazonProductionCredentialsFromEnv())
# and, deliberately, nowhere else yet: pushInventory/confirmShipment against
# production aren't built until Arif has picked a safe test SKU and placed
# a self-test order. Leave these unset until that walkthrough happens.
AMAZON_PRODUCTION_CLIENT_ID=
AMAZON_PRODUCTION_CLIENT_SECRET=
AMAZON_PRODUCTION_REFRESH_TOKEN=
AMAZON_PRODUCTION_SELLER_ID=
# Real marketplace id for Arif's account (e.g. the UK marketplace, if his
# seller account matches this repo's existing UK/EU sandbox assumption) --
# deliberately not defaulted the way the sandbox connector defaults to the
# US marketplace id; the production smoke test's own
# getMarketplaceParticipations() call is the way to find the real value if
# it isn't already known, not a guess baked in here.
AMAZON_PRODUCTION_MARKETPLACE_ID=
# Optional -- override only if Arif's account isn't EU (see
# SP_API_NA_PRODUCTION_BASE_URL / SP_API_FE_PRODUCTION_BASE_URL in
# amazon-connector.ts). Unset defaults to the EU production host.
AMAZON_PRODUCTION_BASE_URL=

# "Connect Amazon" OAuth flow (packages/web/src/app/api/channels/amazon/{connect,callback},
# packages/channel-connectors/src/amazon-oauth.ts) -- the redirect-based
# Website Authorization Workflow, distinct from the AMAZON_SANDBOX_* self-
# authorization credentials above. Left blank: there is no Public SP-API
# application registered for this repo yet (only the Private app the
# AMAZON_SANDBOX_* values above belong to), and per Amazon's docs a Private
# application cannot use this flow at all -- see the header comment in
# amazon-oauth.ts. Leaving these unset makes /api/channels/amazon/connect
# fail loudly with a clear "missing env var" error rather than redirecting
# somewhere broken. Fill in once/if this app is published as Public:
AMAZON_APP_ID=
AMAZON_OAUTH_CLIENT_ID=
AMAZON_OAUTH_CLIENT_SECRET=
# Must exactly match the redirect URI registered for AMAZON_APP_ID in Seller
# Central's Developer Console -- e.g. https://<your-domain>/api/channels/amazon/callback
AMAZON_OAUTH_REDIRECT_URI=
# "false" once AMAZON_APP_ID is a Published (not Draft) application -- see
# amazon-oauth.ts's buildAmazonAuthorizeUrl. Defaults to true (Draft) when unset.
AMAZON_APP_DRAFT=true
# No marketplace comes back on the OAuth callback itself (only
# selling_partner_id does) -- see the callback route's comment. Defaults to
# "US" when unset.
AMAZON_OAUTH_DEFAULT_MARKETPLACE=US
# HMAC key signing the callback's CSRF `state` param
# (packages/web/src/lib/amazon-oauth-state.ts). Generate with e.g.
# `openssl rand -base64 32`. Distinct from CHANNEL_CREDENTIALS_ENCRYPTION_KEY
# below on purpose -- see that file's header comment.
AMAZON_OAUTH_STATE_SECRET=

# packages/channel-connectors/src/shopify-connector.ts -- channel #3
# (CLAUDE.md §8 Phase 3). A Shopify development store's custom-app Admin API
# access token -- structurally simpler than the Amazon/Walmart credentials
# above: no OAuth round trip, no refresh token, a single non-expiring
# shpat_... token that authenticates every Admin API call on its own. Get
# these from a free Shopify Partner account: create a development store,
# then in that store's admin go to Settings -> Apps and sales channels ->
# Develop apps -> Create an app, configure the Admin API scopes this
# connector needs (read_orders, read_products -- pushInventory()'s
# productVariants(sku:...) lookup needs this even though the actual write is
# via write_inventory, not a products scope -- write_inventory, read_locations
# -- pushInventory()'s primaryLocationId() lookup needs this too -- and one of
# write_assigned_fulfillment_orders/write_merchant_managed_fulfillment_orders/
# write_third_party_fulfillment_orders -- see confirmShipment's doc comment
# for why not write_fulfillments), and write_products -- createListing()'s
# productSet mutation, the outbound "create a new listing on Shopify" flow
# (packages/web's /api/channels/shopify/listings route) -- install the app,
# and copy the Admin API access token shown once at install time. These two vars are consumed
# directly by loadShopifyCredentialsFromEnv() and
# scripts/shopify-sandbox-smoke-test.ts for local/manual testing against a
# real dev store; a real tenant's own credentials are typed into
# /settings/channels' "Connect Shopify" form instead and stored per-tenant in
# channel_connections (see CLAUDE.md §4.5's "Wired into the app").
SHOPIFY_SANDBOX_SHOP_DOMAIN=
SHOPIFY_SANDBOX_ACCESS_TOKEN=
# Optional, opt-in (see shopify-sandbox-smoke-test.ts's own comment):
# registers real webhook subscriptions on the dev store above when set, so
# only set this to a callback URL that can actually receive a delivery (an
# ngrok tunnel, a Vercel preview deployment's /api/webhooks/shopify) --
# never a production tenant's callback URL, since this test-registers on the
# *dev store*, not a real tenant's connection.
SHOPIFY_SANDBOX_TEST_WEBHOOK_CALLBACK_URL=
# Optional, opt-in (see shopify-sandbox-smoke-test.ts's own comment):
# exercises ShopifyConnector.createListing() against the dev store above when
# set -- an internal SKU string (doesn't need to already exist as a real
# products row; the smoke test only needs something to pass through as
# internalSku/title). Creates a real product on the dev store every run, so
# leave unset unless you're specifically testing the outbound listing flow.
SHOPIFY_SANDBOX_TEST_CREATE_LISTING_SKU=

# packages/channel-connectors/src/walmart-connector.ts -- the third MVP
# channel (CLAUDE.md §0's locked MVP scope, §8 Phase 3). A Walmart
# Marketplace API client_credentials pair (Client ID + Client Secret),
# issued directly to a Walmart seller/Solution Provider account -- no OAuth
# consent-screen redirect the way Amazon's is, no dev-store-style free
# sandbox the way Shopify's is either. As of this writing there is no
# self-serve Walmart sandbox to register for the way Amazon's SP-API
# sandbox or a Shopify Partner dev store are -- these two vars stay unset
# until this project (or a real tenant) has an approved Walmart
# seller/Solution Provider account, which is also why
# WalmartConnector/createWalmartConnectorFromChannelConnection and every
# route/scheduler job built on top of them (CLAUDE.md §4.2's "Wired into the
# app") remain UNVERIFIED IN PRACTICE despite being fully wired and
# typechecked. These two vars are consumed directly by
# loadWalmartSandboxCredentialsFromEnv() (the default constructor argument
# for `new WalmartConnector()`, same pattern as Shopify's
# loadShopifyCredentialsFromEnv()) and scripts/walmart-sandbox-smoke-test.ts
# for local/manual testing once real credentials exist; a real tenant's own
# credentials are typed into /settings/channels' "Connect Walmart" form
# instead and stored per-tenant in channel_connections, reusing
# lwa_client_id/encrypted_client_secret (see
# createWalmartConnectorFromChannelConnection's own doc comment for why no
# new migration was needed for this reuse).
WALMART_SANDBOX_CLIENT_ID=
WALMART_SANDBOX_CLIENT_SECRET=

# packages/channel-connectors/src/ebay-connector.ts -- channel #4 (CLAUDE.md
# §8 Phase 5's roadmap, picked up early once the connector abstraction had
# proven itself across three structurally different APIs). eBay's own OAuth
# model is a real authorization-code + long-lived-refresh-token grant --
# structurally the closest of the three existing channels to Amazon's own
# shape, not Walmart's client_credentials pair. As with Walmart, there is no
# free self-serve path to a live credential the way Shopify's dev store is:
# a real eBay Developer Program application (sandbox or production keyset)
# is required first. EBAY_SANDBOX_REFRESH_TOKEN is obtained the same
# self-authorization way AMAZON_SANDBOX_REFRESH_TOKEN already is for this
# codebase's Amazon connector -- eBay's own developer dashboard has a
# built-in "Get a Token from eBay via Your Application" tool that hands back
# a refresh token via a one-time manual consent click, no redirect flow
# needed for local/sandbox testing. These three vars are consumed directly
# by loadEbaySandboxCredentialsFromEnv() (the default constructor argument
# for `new EbayConnector()`); a real tenant's own credentials are connected
# through the redirect-based "Connect eBay" OAuth flow instead (see the
# EBAY_OAUTH_* block below) and stored per-tenant in channel_connections,
# reusing lwa_client_id/encrypted_client_secret/encrypted_refresh_token --
# no new migration needed (same reuse WalmartConnector's own connect route
# already established, extended here to the full refresh-token shape
# Amazon's own row already uses).
#
# UNVERIFIED IN PRACTICE more so even than Walmart's own wiring: THIS REPO'S
# OWN Claude Code cloud sandbox's outbound network policy blocks BOTH
# api.ebay.com and api.sandbox.ebay.com entirely (confirmed directly -- a
# token-exchange curl to each host was rejected at the proxy level), so
# nothing eBay-related in this codebase has been exercised against live
# infrastructure of any kind FROM THAT ENVIRONMENT, unlike Amazon (proven
# against its SP-API sandbox) or Shopify (proven against a real dev store).
# That block is specific to this project's own cloud dev environment, not a
# statement about eBay's own network reachability in general -- run
# `npm run ebay:sandbox-smoke-test` from a normal machine (a developer's own
# laptop, or once deployed) once real credentials are filled in below.
#
# How to get sandbox credentials (no eBay seller account required for this
# part -- just a regular eBay account):
#   1. Join the eBay Developers Program: https://developer.ebay.com/join/
#   2. Your Account -> Application Keys -- creates a Sandbox keyset (App ID =
#      EBAY_SANDBOX_CLIENT_ID, Cert ID = EBAY_SANDBOX_CLIENT_SECRET) separate
#      from its Production keyset.
#   3. Next to the Sandbox Client ID, click User Tokens -> "Get a Token from
#      eBay via Your Application" -- a one-time manual consent click (no
#      redirect server needed) that hands back a refresh token directly, the
#      same self-authorization shape AMAZON_SANDBOX_REFRESH_TOKEN already
#      relies on. When it asks which scopes to grant, check all three this
#      codebase's EBAY_OAUTH_SCOPES requests: sell.fulfillment,
#      sell.inventory, sell.account.readonly. That token is
#      EBAY_SANDBOX_REFRESH_TOKEN below.
#   4. Optional, for realistic pullOrders() data: still under User Tokens,
#      "Register a new Sandbox user" creates a fake seller account
#      (username prefixed TESTUSER_) with its own sandbox orders to pull --
#      your own real eBay account has no sandbox data of its own.
EBAY_SANDBOX_CLIENT_ID=
EBAY_SANDBOX_CLIENT_SECRET=
EBAY_SANDBOX_REFRESH_TOKEN=

# Everything below is optional and purely for scripts/ebay-sandbox-smoke-test.ts's
# own opt-in steps -- see that file's header comment for exactly which var(s)
# gate which real, persisting write. Leave all of them unset to only run the
# script's read-only steps (authenticate, pullOrders, fetchBusinessPolicies).
#
# Feeds EbayConnector's own fulfillmentPolicyId/paymentPolicyId/
# returnPolicyId/merchantLocationKey credential fields (see
# EbayCredentials' own doc comment) -- fill these in with real ids the
# smoke test's fetchBusinessPolicies() step prints out (or ids returned by
# the createMerchantLocation() opt-in step below, once you've run it once)
# to then opt into the createListing() step further down.
EBAY_SANDBOX_TEST_FULFILLMENT_POLICY_ID=
EBAY_SANDBOX_TEST_PAYMENT_POLICY_ID=
EBAY_SANDBOX_TEST_RETURN_POLICY_ID=

# Opt into pushInventory() against this real sandbox SKU.
EBAY_SANDBOX_TEST_SKU=

# Opt into confirmShipment() against this real sandbox order id (obtained
# from a pullOrders() run above, or eBay's own sandbox order-creation tools).
EBAY_SANDBOX_TEST_ORDER_ID=
EBAY_SANDBOX_TEST_CARRIER=
EBAY_SANDBOX_TEST_TRACKING_NUMBER=

# Opt into createMerchantLocation() -- set the key plus every address field
# together (all six, or none of them).
EBAY_SANDBOX_TEST_MERCHANT_LOCATION_KEY=
EBAY_SANDBOX_TEST_LOCATION_NAME=
EBAY_SANDBOX_TEST_LOCATION_ADDRESS_LINE1=
EBAY_SANDBOX_TEST_LOCATION_CITY=
EBAY_SANDBOX_TEST_LOCATION_STATE=
EBAY_SANDBOX_TEST_LOCATION_POSTAL_CODE=
EBAY_SANDBOX_TEST_LOCATION_COUNTRY=

# Opt into createListing() -- set the SKU plus every listing field together
# (all six, or none of them). Requires the three policy id vars and the
# merchant location key above to already be set, same fail-fast requirement
# createListing() itself enforces for a real tenant's incomplete Selling
# Setup.
EBAY_SANDBOX_TEST_LISTING_SKU=
EBAY_SANDBOX_TEST_LISTING_TITLE=
EBAY_SANDBOX_TEST_LISTING_DESCRIPTION=
EBAY_SANDBOX_TEST_LISTING_IMAGE_URL=
EBAY_SANDBOX_TEST_LISTING_CATEGORY_ID=
EBAY_SANDBOX_TEST_LISTING_PRICE=

# "Connect eBay" OAuth flow
# (packages/web/src/app/api/channels/ebay/{connect,callback},
# packages/channel-connectors/src/ebay-oauth.ts) -- the redirect-based
# Authorization Code Grant, distinct from the EBAY_SANDBOX_* self-
# authorization credentials above. Left blank: no eBay Developer Program
# application is registered for this repo yet. Leaving these unset makes
# /api/channels/ebay/connect fail loudly with a clear "missing env var"
# error rather than redirecting somewhere broken, same as Amazon's own
# connect route. Fill in once a real application exists:
EBAY_OAUTH_CLIENT_ID=
EBAY_OAUTH_CLIENT_SECRET=
# eBay calls this an "RuName" (redirect URL name), not a literal callback
# URL -- it's a value eBay itself issues when you register a redirect URL in
# the Developer Program's keyset settings, not something you choose
# yourself the way AMAZON_OAUTH_REDIRECT_URI's literal URL is.
EBAY_OAUTH_REDIRECT_URI=
# "true" to send the "Connect eBay" flow to eBay's sandbox consent
# screen/token host instead of production -- unlike Amazon's *_APP_DRAFT
# (a property of the registered application itself), this is purely a
# which-host choice. Defaults to production (false/unset) when unset.
EBAY_OAUTH_SANDBOX=
# HMAC key signing the callback's CSRF `state` param
# (packages/web/src/lib/ebay-oauth-state.ts). Generate with e.g.
# `openssl rand -base64 32`. A separate secret from
# AMAZON_OAUTH_STATE_SECRET on purpose -- see that module's own header
# comment for why each channel gets its own.
EBAY_OAUTH_STATE_SECRET=

# Real-time Shopify webhooks (packages/web/src/app/api/webhooks/shopify,
# ShopifyConnector.registerWebhooks -- CLAUDE.md §4.5). The full, public
# URL every connected tenant's store is told to POST orders/create,
# orders/cancelled, and app/uninstalled deliveries to -- one shared endpoint
# for every tenant (the delivery's own X-Shopify-Shop-Domain header is what
# the route resolves back to a specific tenant), so this is a single
# deployment-wide URL, not a per-tenant value the way AMAZON_OAUTH_REDIRECT_URI
# is. Same "one full, explicit URL, never derived from request headers"
# convention as that var. Optional: when unset, /api/channels/shopify/connect
# still connects a store fine, it just skips webhook registration and that
# tenant stays on the daily cron (packages/scheduler) only -- see the connect
# route's own doc comment.
SHOPIFY_WEBHOOK_CALLBACK_URL=

# Symmetric key pgcrypto uses to encrypt/decrypt
# channel_connections.encrypted_client_secret / encrypted_refresh_token /
# encrypted_access_token (see packages/db/migrations/0012_channel_connections.sql,
# 0019_channel_connections_shopify.sql, and packages/db/src/encryption.ts) --
# encrypted_client_secret now holds a Shopify tenant's webhook signing secret
# too (0019 left it nullable for Amazon-OAuth reasons; a per-tenant custom
# app's client secret reuses it since webhooks were added, see
# /api/channels/shopify/connect's own doc comment -- no new column). Interim
# stand-in for the KMS-backed encryption CLAUDE.md §6 calls for -- no KMS
# integration exists yet. Rotating this value requires re-encrypting existing
# rows with the new key.
CHANNEL_CREDENTIALS_ENCRYPTION_KEY=

# Billing/Subscription (CLAUDE.md §1, §5 Stripe Billing) -- @alltix/billing-service.
# TEST-MODE KEYS ONLY (sk_test_.../whsec_.../price_... created in test mode).
# There is exactly one tenant self-testing this right now, not a paying
# customer -- no reason to ever put a live-mode key here.
STRIPE_SECRET_KEY=
# From the Stripe Dashboard's webhook endpoint config, or printed by
# `stripe listen --forward-to <url>/api/webhooks/stripe` for local testing
# (that command prints its own whsec_... to use while it's running).
STRIPE_WEBHOOK_SECRET=
# One flat MVP-plan Price id (CLAUDE.md §8 Phase 2: one tier, not a matrix
# yet) -- create it with `npm run stripe:setup-mvp-plan`, which is
# idempotent and prints the id to save here.
STRIPE_MVP_PRICE_ID=

# Vercel Cron Jobs (vercel.json's `crons` entry) -- secures
# GET /api/cron/amazon-order-sync so only Vercel's own scheduled invocation
# can trigger a sync pass, per Vercel's documented pattern
# (https://vercel.com/docs/cron-jobs/manage-cron-jobs#securing-cron-jobs):
# set this in the Vercel project's env vars (a random 16+ char string --
# a password generator works fine) and Vercel automatically sends it back as
# `Authorization: Bearer <value>` on every cron invocation. Unset locally is
# fine -- the route 401s with no secret configured rather than accepting
# unauthenticated requests, and local runs go through
# `npm run amazon:order-sync-job` instead, same as before this existed.
CRON_SECRET=

# Marketing site "Book a Demo" lead capture
# (src/app/(marketing)/book-a-demo, src/app/api/leads/demo-request). Every
# submission is always saved to the demo_requests table regardless of these
# -- they only control the *optional* best-effort email notification via
# Resend's HTTP API (https://resend.com). Leave both unset to skip email
# entirely and rely on querying demo_requests directly.
RESEND_API_KEY=
# Where new demo-request notifications get sent.
DEMO_REQUEST_NOTIFY_EMAIL=
# Optional -- must be a domain verified in Resend to actually deliver;
# defaults to Resend's own sandbox sender otherwise.
DEMO_REQUEST_FROM_EMAIL=

# Observability (CLAUDE.md §5/§11/§8 Phase 4 -- "Observability dashboards")
# -- Sentry, chosen over Datadog/Grafana+Prometheus because it's a hosted
# SaaS needing no new infra on this app's Vercel Hobby plan, and matches
# §5/§11's "why did SKU X oversell at 3am" debugging story directly (a
# real stack trace + tenant/order context beats a log line grep). Wired in
# now with every DSN left unset, same "wire it now, verify against a real
# account later" pattern as every marketplace connector's own credentials
# in this file -- Sentry.init() with no dsn is a confirmed no-op (silently
# never sends events, see packages/shared/src/observability.ts's own header
# comment), so the app runs identically with or without an account.
#
# packages/web (Next.js, @sentry/nextjs) -- two separate DSNs because
# NEXT_PUBLIC_-prefixed vars are compiled into the public browser bundle;
# a bare SENTRY_DSN read from src/instrumentation-client.ts would always be
# undefined client-side. Both would normally be the *same* DSN value from
# one Sentry project -- split into two vars here only because Next.js
# requires the client one to carry the NEXT_PUBLIC_ prefix, not because
# they're meant to point at different projects.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
# Everything below is optional and only affects build-time source-map
# upload (next.config.mjs's withSentryConfig) -- unset, the build still
# succeeds, it just uploads no source maps, so a Sentry stack trace shows
# minified code until these are set.
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Non-web packages (scheduler's node-cron scripts, the one-shot job scripts
# under /scripts) -- plain @sentry/node via packages/shared/src/
# observability.ts's initObservability()/captureError()/captureAlert(),
# NOT @sentry/nextjs (Next.js's SDK/file conventions don't apply outside
# packages/web). Deliberately the SAME SENTRY_DSN var as packages/web's
# server-side one above, not a separate one -- one Sentry project for the
# whole backend is enough at this stage, and every event already carries a
# `service` tag (see initObservability's own doc comment) to tell a
# scheduler failure apart from a web-server one. Vercel Cron invocations
# (GET /api/cron/*-order-sync) run inside packages/web's own Next.js
# server process and are already covered by SENTRY_DSN above -- this
# section only matters for the separate, non-Next `npm run *:order-sync-job`
# / `npm run *:order-sync-scheduler` processes.